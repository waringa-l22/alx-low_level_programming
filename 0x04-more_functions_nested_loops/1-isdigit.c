#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int main(void)
{
	char c;

	c= '0';

	     c='a';
	         printf("\nResult when non-numeric character is passed: %d", _isdigit(c));

return 0;
}
